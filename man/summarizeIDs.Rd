% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanPedigree.R
\name{summarizeIDs}
\alias{summarizeIDs}
\alias{summariseIDs}
\title{Summarize Where One or More IDs Occur Across a List of Data Frames}
\usage{
summarizeIDs(data_list, ID, ...)

summariseIDs(data_list, ID, ...)
}
\arguments{
\item{data_list}{A named list of data frames to search.}

\item{ID}{A vector of one or more IDs to search for.}

\item{...}{Additional arguments passed to [findIDs()].}
}
\value{
A data frame with columns `matched_id`, `dataset`, `matched_column`,
  and `n_matches`.
}
\description{
A count-level companion to [findIDs()]: instead of one row per hit, returns
one row per (id, dataset, column) with the number of matches. Useful for a
quick census of how many places an ID appears before drilling in with
[findIDs()].
}
\examples{
\dontrun{
summarizeIDs(list(clean = clean_df, raw = raw_df), ID = 389785)
}
}
